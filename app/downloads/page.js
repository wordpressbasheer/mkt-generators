import Header from '../../components/Header';import Footer from '../../components/Footer';import {generators,engineBrands} from '../../data/generators';
export default function Downloads(){return <main><Header/><section className="page-hero"><span>DOWNLOADS</span><h1>MKT Datasheet Library</h1><p>All public downloads are branded as MKT. Use Print / Save PDF inside each datasheet page.</p></section><section className="downloads">{engineBrands.map(b=><div className="download-group" key={b}><h2>{b} Series</h2><div className="download-list">{generators.filter(g=>g.engineBrand===b).map(g=><a key={g.id} href={`/datasheets/${g.slug}`}><b>{g.model}</b><span>{g.standbyKva} kVA</span></a>)}</div></div>)}</section><Footer/></main>}
