'use client';import {useEffect,useState} from 'react';import Header from '../../components/Header';import Footer from '../../components/Footer';
export default function RFQ(){const [items,setItems]=useState([]);useEffect(()=>setItems(JSON.parse(localStorage.getItem('mkt_quote_items')||'[]')),[]);return <main><Header/><section className="page-hero"><span>RFQ</span><h1>Request a Quotation</h1><p>Send your selected models and project requirements.</p></section><section className="rfq"><div className="selected"><h3>Selected Generators</h3>{items.length?items.map(i=><div className="quote-item" key={i.id}><b>{i.model}</b><small>{i.brand} · {i.kva} kVA</small></div>):<p>No models selected yet. You can still send a general inquiry.</p>}</div><form action="mailto:sales@mktgenerators.com" method="post" encType="text/plain"><label>Name<input name="name" required/></label><label>Company<input name="company"/></label><label>Email<input name="email" type="email" required/></label><label>Phone / WhatsApp<input name="phone"/></label><label>Project Details<textarea name="message" rows="7" defaultValue={items.map(i=>`${i.model} - ${i.brand} - ${i.kva} kVA`).join('\n')}/></label><button>Send RFQ</button></form></section><Footer/></main>}
