'use client';
import {useEffect,useState} from 'react';
export default function QuoteDrawer(){const [open,setOpen]=useState(false),[items,setItems]=useState([]); const load=()=>setItems(JSON.parse(localStorage.getItem('mkt_quote_items')||'[]')); useEffect(()=>{load(); window.addEventListener('mktQuoteUpdated',load); return()=>window.removeEventListener('mktQuoteUpdated',load)},[]); function clear(){localStorage.removeItem('mkt_quote_items'); load()}
return <><button className="floating-quote" onClick={()=>setOpen(true)}>RFQ List <b>{items.length}</b></button><aside className={`drawer ${open?'open':''}`}><div className="drawer-head"><h3>Quote Request</h3><button onClick={()=>setOpen(false)}>×</button></div>{items.length?items.map(i=><div className="quote-item" key={i.id}><b>{i.model}</b><small>{i.brand} · {i.kva} kVA</small></div>):<p>No generators selected.</p>}<a className="quote-submit" href="/rfq">Complete RFQ</a>{items.length?<button className="clear-btn" onClick={clear}>Clear List</button>:null}</aside></>}
